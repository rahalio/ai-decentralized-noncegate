/**
 * Webhook View
 *
 * Main view component for webhook domain.
 */

import { WebhookViewProps } from "./types";

export function WebhookView({}: WebhookViewProps) {
  return (
    <div>
      <h1>Webhook View</h1>
      <p>This is a generated view component for the webhook domain.</p>
      {/* TODO: Implement view */}
    </div>
  );
}
