/**
 * GetVerificationResult handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: verification-result
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getVerificationResult } from "@noncegate/api-server/domains/verification-result/handlers";
import type { VerificationResultDDDDependencies } from "@noncegate/api-server/domains/verification-result";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@noncegate/tests/shared";
import { VERIFICATIONRESULT_USE_CASES } from "../__factories__/use-cases.js";

describe("GetVerificationResult", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: VerificationResultDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<VerificationResultDDDDependencies>(
      VERIFICATIONRESULT_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetVerificationResult, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { resultId: entity.id };

    await getVerificationResult(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetVerificationResult.execute).toHaveBeenCalledWith(expect.objectContaining({ resultId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { resultId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetVerificationResult, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getVerificationResult(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
