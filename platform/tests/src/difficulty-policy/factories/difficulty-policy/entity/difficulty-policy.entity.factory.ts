/**
 * DifficultyPolicy Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: difficulty-policy
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { DifficultyPolicyEntity } from "@noncegate/core/difficulty-policy/models/index.js";
import { difficultyPolicySchemas } from "@noncegate/core/difficulty-policy";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "dpol";

export function createDifficultyPolicy(overrides: Partial<DifficultyPolicyEntity> = {}) {
  const raw = {
    id: overrides.id ?? `pol_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    difficulty: overrides.difficulty ?? faker.number.int(),
    effectiveAt: overrides.effectiveAt ?? new Date().toISOString(),
    leadingZeroTarget: overrides.leadingZeroTarget ?? faker.number.int(),
    status: overrides.status ?? "draft",
    version: overrides.version ?? faker.number.int(),
    ...overrides,
  };

  return difficultyPolicySchemas.DifficultyPolicy.parse(raw);
}

export function difficultyPolicyBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createDifficultyPolicy(overrides));
}
