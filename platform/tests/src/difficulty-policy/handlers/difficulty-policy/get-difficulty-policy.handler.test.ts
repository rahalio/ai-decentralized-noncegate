/**
 * GetDifficultyPolicy handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: difficulty-policy
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getDifficultyPolicy } from "@noncegate/api-server/domains/difficulty-policy/handlers";
import type { DifficultyPolicyDDDDependencies } from "@noncegate/api-server/domains/difficulty-policy";
import { createDifficultyPolicy as createDifficultyPolicyEntity } from "@noncegate/factories/difficulty-policy";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@noncegate/tests/shared";
import { DIFFICULTYPOLICY_USE_CASES } from "../__factories__/use-cases.js";

describe("GetDifficultyPolicy", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: DifficultyPolicyDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<DifficultyPolicyDDDDependencies>(
      DIFFICULTYPOLICY_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createDifficultyPolicyEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetDifficultyPolicy, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { policyId: entity.id };

    await getDifficultyPolicy(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetDifficultyPolicy.execute).toHaveBeenCalledWith(expect.objectContaining({ policyId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { policyId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetDifficultyPolicy, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getDifficultyPolicy(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
