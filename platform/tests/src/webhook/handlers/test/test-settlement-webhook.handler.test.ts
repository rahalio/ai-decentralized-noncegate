/**
 * TestSettlementWebhook handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: webhook
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { testSettlementWebhook } from "@noncegate/api-server/domains/webhook/handlers";
import type { WebhookDDDDependencies } from "@noncegate/api-server/domains/webhook";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@noncegate/tests/shared";
import { WEBHOOK_USE_CASES } from "../__factories__/use-cases.js";

describe("TestSettlementWebhook", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: WebhookDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<WebhookDDDDependencies>(
      WEBHOOK_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeTestSettlementWebhook, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { webhookId: entity.id };

    await testSettlementWebhook(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeTestSettlementWebhook.execute).toHaveBeenCalledWith(expect.objectContaining({ webhookId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { webhookId: nonExistentId };
    
    vi.spyOn(mockDeps.executeTestSettlementWebhook, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      testSettlementWebhook(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
