/**
 * GetForkReport handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: fork-report
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getForkReport } from "@noncegate/api-server/domains/fork-report/handlers";
import type { ForkReportDDDDependencies } from "@noncegate/api-server/domains/fork-report";
import { createForkReport as createForkReportEntity } from "@noncegate/factories/fork-report";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@noncegate/tests/shared";
import { FORKREPORT_USE_CASES } from "../__factories__/use-cases.js";

describe("GetForkReport", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ForkReportDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ForkReportDDDDependencies>(
      FORKREPORT_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createForkReportEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetForkReport, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { forkId: entity.id };

    await getForkReport(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetForkReport.execute).toHaveBeenCalledWith(expect.objectContaining({ forkId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { forkId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetForkReport, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getForkReport(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
