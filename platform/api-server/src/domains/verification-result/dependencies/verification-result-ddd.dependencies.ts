/**
 * VerificationResult DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: verification-result
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * VerificationResult Domain Module - Composition root
 *
 * DDD: Composition root for verification-result domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  ReverifyRepositoryAdapter,
  VerificationResultRepositoryAdapter,
} from "@noncegate/adapters/verification-result";
import { getIdGeneratorService } from "@noncegate/adapters";
import type { AdapterDynamoDBClient } from "@noncegate/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteCreateVerificationResult,
  ExecuteGetVerificationResult,
  ExecuteListVerificationResults,
  ExecuteReverifySample,
} from "@noncegate/services/verification-result/usecases";
// Ports (interfaces)
import type {
  ReverifyRepository,
  VerificationResultRepository,
} from "@noncegate/services/verification-result/ports";

export interface VerificationResultDomainModule {
  repos: {
    reverifies: ReverifyRepository;
    verificationResults: VerificationResultRepository;
  };
  useCases: {
    reverifies: {
      get: ExecuteReverifySample;
    };
    verificationResults: {
      create: ExecuteCreateVerificationResult;
      get: ExecuteGetVerificationResult;
      list: ExecuteListVerificationResults;
    };
  };
}

export function buildVerificationResultDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): VerificationResultDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      reverifies?: ReverifyRepository;
      verificationResults?: VerificationResultRepository;
    } = {};

    obj.reverifies = new ReverifyRepositoryAdapter(dynamoClient);
    obj.verificationResults = new VerificationResultRepositoryAdapter(dynamoClient);
    return {
      reverifies: obj.reverifies!,
      verificationResults: obj.verificationResults!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    reverifies: {
      get: new ExecuteReverifySample(executionContext, idGenerator, repos.reverifies),
    },
    verificationResults: {
      create: new ExecuteCreateVerificationResult(executionContext, idGenerator, repos.verificationResults),
      get: new ExecuteGetVerificationResult(executionContext, idGenerator, repos.verificationResults),
      list: new ExecuteListVerificationResults(executionContext, idGenerator, repos.verificationResults),
    },
  };
  return { repos, useCases: useCases as VerificationResultDomainModule["useCases"] };
}
