/**
 * Webhook DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: webhook
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Webhook Domain Module - Composition root
 *
 * DDD: Composition root for webhook domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  DeliveryRepositoryAdapter,
  PauseRepositoryAdapter,
  ReplayRepositoryAdapter,
  TestRepositoryAdapter,
  WebhookRepositoryAdapter,
} from "@noncegate/adapters/webhook";
import { getIdGeneratorService } from "@noncegate/adapters";
import type { AdapterDynamoDBClient } from "@noncegate/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteCreateSettlementWebhook,
  ExecuteGetSettlementWebhook,
  ExecuteListSettlementWebhooks,
  ExecuteListWebhookDeliveries,
  ExecutePauseSettlementWebhook,
  ExecuteReplayWebhookDelivery,
  ExecuteTestSettlementWebhook,
} from "@noncegate/services/webhook/usecases";
// Ports (interfaces)
import type {
  DeliveryRepository,
  PauseRepository,
  ReplayRepository,
  TestRepository,
  WebhookRepository,
} from "@noncegate/services/webhook/ports";

export interface WebhookDomainModule {
  repos: {
    deliveries: DeliveryRepository;
    pauses: PauseRepository;
    replays: ReplayRepository;
    tests: TestRepository;
    webhooks: WebhookRepository;
  };
  useCases: {
    deliveries: {
      list: ExecuteListWebhookDeliveries;
    };
    pauses: {
      get: ExecutePauseSettlementWebhook;
    };
    replays: {
      get: ExecuteReplayWebhookDelivery;
    };
    tests: {
      get: ExecuteTestSettlementWebhook;
    };
    webhooks: {
      create: ExecuteCreateSettlementWebhook;
      get: ExecuteGetSettlementWebhook;
      list: ExecuteListSettlementWebhooks;
    };
  };
}

export function buildWebhookDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): WebhookDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      deliveries?: DeliveryRepository;
      pauses?: PauseRepository;
      replays?: ReplayRepository;
      tests?: TestRepository;
      webhooks?: WebhookRepository;
    } = {};

    obj.deliveries = new DeliveryRepositoryAdapter(dynamoClient);
    obj.pauses = new PauseRepositoryAdapter(dynamoClient);
    obj.replays = new ReplayRepositoryAdapter(dynamoClient);
    obj.tests = new TestRepositoryAdapter(dynamoClient);
    obj.webhooks = new WebhookRepositoryAdapter(dynamoClient);
    return {
      deliveries: obj.deliveries!,
      pauses: obj.pauses!,
      replays: obj.replays!,
      tests: obj.tests!,
      webhooks: obj.webhooks!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    deliveries: {
      list: new ExecuteListWebhookDeliveries(executionContext, idGenerator, repos.deliveries),
    },
    pauses: {
      get: new ExecutePauseSettlementWebhook(executionContext, idGenerator, repos.pauses),
    },
    replays: {
      get: new ExecuteReplayWebhookDelivery(executionContext, idGenerator, repos.replays),
    },
    tests: {
      get: new ExecuteTestSettlementWebhook(executionContext, idGenerator, repos.tests),
    },
    webhooks: {
      create: new ExecuteCreateSettlementWebhook(executionContext, idGenerator, repos.webhooks),
      get: new ExecuteGetSettlementWebhook(executionContext, idGenerator, repos.webhooks),
      list: new ExecuteListSettlementWebhooks(executionContext, idGenerator, repos.webhooks),
    },
  };
  return { repos, useCases: useCases as WebhookDomainModule["useCases"] };
}
