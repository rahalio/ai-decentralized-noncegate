/**
 * Alert DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: alert
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Alert Domain Module - Composition root
 *
 * DDD: Composition root for alert domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  AckRepositoryAdapter,
  AlertRepositoryAdapter,
  EscalateRepositoryAdapter,
  ResolveRepositoryAdapter,
  SilenceRepositoryAdapter,
} from "@noncegate/adapters/alert";
import { getIdGeneratorService } from "@noncegate/adapters";
import type { AdapterDynamoDBClient } from "@noncegate/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteAckAlert,
  ExecuteCreateAlert,
  ExecuteEscalateAlert,
  ExecuteGetAlert,
  ExecuteListAlerts,
  ExecuteResolveAlert,
  ExecuteSilenceAlert,
} from "@noncegate/services/alert/usecases";
// Ports (interfaces)
import type {
  AckRepository,
  AlertRepository,
  EscalateRepository,
  ResolveRepository,
  SilenceRepository,
} from "@noncegate/services/alert/ports";

export interface AlertDomainModule {
  repos: {
    acks: AckRepository;
    alerts: AlertRepository;
    escalates: EscalateRepository;
    resolves: ResolveRepository;
    silences: SilenceRepository;
  };
  useCases: {
    acks: {
      get: ExecuteAckAlert;
    };
    alerts: {
      create: ExecuteCreateAlert;
      get: ExecuteGetAlert;
      list: ExecuteListAlerts;
    };
    escalates: {
      get: ExecuteEscalateAlert;
    };
    resolves: {
      get: ExecuteResolveAlert;
    };
    silences: {
      get: ExecuteSilenceAlert;
    };
  };
}

export function buildAlertDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): AlertDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      acks?: AckRepository;
      alerts?: AlertRepository;
      escalates?: EscalateRepository;
      resolves?: ResolveRepository;
      silences?: SilenceRepository;
    } = {};

    obj.acks = new AckRepositoryAdapter(dynamoClient);
    obj.alerts = new AlertRepositoryAdapter(dynamoClient);
    obj.escalates = new EscalateRepositoryAdapter(dynamoClient);
    obj.resolves = new ResolveRepositoryAdapter(dynamoClient);
    obj.silences = new SilenceRepositoryAdapter(dynamoClient);
    return {
      acks: obj.acks!,
      alerts: obj.alerts!,
      escalates: obj.escalates!,
      resolves: obj.resolves!,
      silences: obj.silences!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    acks: {
      get: new ExecuteAckAlert(executionContext, idGenerator, repos.acks),
    },
    alerts: {
      create: new ExecuteCreateAlert(executionContext, idGenerator, repos.alerts),
      get: new ExecuteGetAlert(executionContext, idGenerator, repos.alerts),
      list: new ExecuteListAlerts(executionContext, idGenerator, repos.alerts),
    },
    escalates: {
      get: new ExecuteEscalateAlert(executionContext, idGenerator, repos.escalates),
    },
    resolves: {
      get: new ExecuteResolveAlert(executionContext, idGenerator, repos.resolves),
    },
    silences: {
      get: new ExecuteSilenceAlert(executionContext, idGenerator, repos.silences),
    },
  };
  return { repos, useCases: useCases as AlertDomainModule["useCases"] };
}
