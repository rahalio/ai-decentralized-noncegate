/**
 * ExecuteIngestBlockObservation - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: block-observation
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteIngestBlockObservation Use Case
 *
 * DDD: Application use case for get BlockObservation.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { IngestBlockObservationInput, IngestBlockObservationOutput } from "../dto/block-observation.dto";
import type { ExecutionContextService, IdGeneratorService } from "@noncegate/services/_shared/index.js";
import type { BlockObservationRepository } from "../ports";
import { NotFoundError } from "../errors";

export class ExecuteIngestBlockObservation {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly blockObservation: BlockObservationRepository
  ) {}

  async execute(input: IngestBlockObservationInput): Promise<IngestBlockObservationOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.bobId();

    // All field names extracted from OpenAPI spec: blockHash, clientLabel, difficultyBits, headerOnly, height, nonce, observedAt, parentHash
    // Load entity via repository (no ID field - uses input directly, pass correlation ID for response meta)
    const entity = await this.blockObservation.ingestBlockObservation({ ...input, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found`);
    }

    return entity as IngestBlockObservationOutput;
  }
}
