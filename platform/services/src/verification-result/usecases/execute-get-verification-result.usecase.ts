/**
 * ExecuteGetVerificationResult - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: verification-result
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetVerificationResult Use Case
 *
 * DDD: Application use case for get VerificationResult.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetVerificationResultInput, GetVerificationResultOutput } from "../dto/verification-result.dto";
import type { ExecutionContextService, IdGeneratorService } from "@noncegate/services/_shared/index.js";
import type { VerificationResultRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetVerificationResult {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly verificationResult: VerificationResultRepository
  ) {}

  async execute(input: GetVerificationResultInput): Promise<GetVerificationResultOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.vrfId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).resultId) {
      throw new ValidationError("resultId is required");
    }
    const resultId = (input as any).resultId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.verificationResult.getVerificationResult({ resultId: resultId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${resultId}`);
    }

    return entity as GetVerificationResultOutput;
  }
}
