/**
 * ExecuteTestSettlementWebhook - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: webhook
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteTestSettlementWebhook Use Case
 *
 * DDD: Application use case for get Test.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { TestSettlementWebhookInput, TestSettlementWebhookOutput } from "../dto/test.dto";
import type { ExecutionContextService, IdGeneratorService } from "@noncegate/services/_shared/index.js";
import type { TestRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteTestSettlementWebhook {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly test: TestRepository
  ) {}

  async execute(input: TestSettlementWebhookInput): Promise<TestSettlementWebhookOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.whkId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).webhookId) {
      throw new ValidationError("webhookId is required");
    }
    const webhookId = (input as any).webhookId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.test.testSettlementWebhook({ webhookId: webhookId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${webhookId}`);
    }

    return entity as TestSettlementWebhookOutput;
  }
}
