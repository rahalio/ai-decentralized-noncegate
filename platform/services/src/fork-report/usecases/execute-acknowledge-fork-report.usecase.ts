/**
 * ExecuteAcknowledgeForkReport - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: fork-report
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteAcknowledgeForkReport Use Case
 *
 * DDD: Application use case for get Acknowledge.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { AcknowledgeForkReportInput, AcknowledgeForkReportOutput } from "../dto/acknowledge.dto";
import type { ExecutionContextService, IdGeneratorService } from "@noncegate/services/_shared/index.js";
import type { AcknowledgeRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteAcknowledgeForkReport {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly acknowledge: AcknowledgeRepository
  ) {}

  async execute(input: AcknowledgeForkReportInput): Promise<AcknowledgeForkReportOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.frkId();

    // All field names extracted from OpenAPI spec: incidentNotes
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).forkId) {
      throw new ValidationError("forkId is required");
    }
    const forkId = (input as any).forkId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.acknowledge.acknowledgeForkReport({ forkId: forkId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${forkId}`);
    }

    return entity as AcknowledgeForkReportOutput;
  }
}
